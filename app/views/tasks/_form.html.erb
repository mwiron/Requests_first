<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
   <b> <%= f.label :task, "TaskNR:" %></b> <%= @task.task %>
  </br >  <%= f.number_field :task, :readonly => true, :hidden => true  %>
   
  </div>
  <!-- <b>Tasknr:</b> -->
  
</p>
  <div class="field">
   <b> <%= f.label :beschreibung, "Task Beschreibung:" %></b><br />
    <%= f.text_field :beschreibung %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
